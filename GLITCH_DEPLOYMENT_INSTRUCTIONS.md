# تعليمات نشر المشروع على Glitch

## الخطوة 1: إنشاء مشروع جديد على Glitch

1. قم بزيارة [Glitch](https://glitch.com/)
2. قم بتسجيل الدخول أو إنشاء حساب جديد
3. انقر على "New Project" ثم اختر "hello-node" (هذا مهم لأننا نستخدم Node.js)

## الخطوة 2: تحضير المشروع

1. بعد إنشاء المشروع، انتظر حتى يتم تحميله بالكامل
2. انقر على "Tools" في الزاوية السفلية اليسرى، ثم اختر "Terminal"
3. في Terminal، قم بحذف الملفات الافتراضية:
   ```
   rm -rf ./*
   ```

## الخطوة 3: رفع الملفات

1. انقر على "Assets" في الشريط الجانبي
2. اضغط على "Upload an Asset"
3. قم برفع الملفات التالية من مشروعك (يمكنك رفعها كملفات مضغوطة ثم استخراجها):
   - مجلد `public`
   - مجلد `server`
   - ملف `package.json`
   - ملف `package-lock.json`
   - ملف `glitch.json`
   - ملف `README.md`
   - ملف `.gitignore`
   - ملف `glitch-setup.js`

4. إذا قمت برفع ملف مضغوط، استخدم Terminal لاستخراجه:
   ```
   unzip your-file.zip
   ```

## الخطوة 4: تكوين المشروع

1. في Terminal، قم بتثبيت التبعيات:
   ```
   npm install
   ```

2. تأكد من أن ملف `package.json` يحتوي على سكربت `start` صحيح:
   ```json
   "scripts": {
     "start": "node server/server.js",
     "prestart": "node glitch-setup.js"
   }
   ```

3. تأكد من أن مجلد `server` موجود وأنه يحتوي على ملفات `server.js` و `database.js`

4. قم بتشغيل سكربت الإعداد:
   ```
   node glitch-setup.js
   ```

## الخطوة 5: تشغيل المشروع

1. في Terminal، قم بتشغيل المشروع:
   ```
   npm start
   ```

2. انقر على "Show" في الزاوية العلوية اليسرى
3. اختر "In a New Window" لفتح المشروع في نافذة جديدة

## الخطوة 6: التحقق من قاعدة البيانات

1. إذا واجهت مشاكل مع قاعدة البيانات، تأكد من أن مجلد `server` يحتوي على ملف `university.db`
2. إذا لم يكن موجودًا، سيتم إنشاؤه تلقائيًا عند تشغيل المشروع
3. يمكنك التحقق من حالة قاعدة البيانات في Terminal:
   ```
   ls -la server/
   ```

## ملاحظات هامة

1. **المسارات**: تأكد من أن جميع المسارات في الكود تستخدم `path.join` بدلاً من المسارات الثابتة
2. **البيئة**: Glitch يستخدم بيئة Linux، لذا تأكد من أن الكود متوافق مع Linux
3. **الملفات المؤقتة**: Glitch قد يعيد تشغيل المشروع بشكل دوري، لذا لا تعتمد على الملفات المؤقتة
4. **الذاكرة**: Glitch لديه حدود للذاكرة، لذا تجنب العمليات كثيفة الاستخدام للذاكرة

## استكشاف الأخطاء وإصلاحها

1. **مشاكل قاعدة البيانات**: إذا واجهت مشاكل مع قاعدة البيانات، جرب حذف ملف `university.db` وإعادة تشغيل المشروع
2. **مشاكل التبعيات**: إذا واجهت مشاكل مع التبعيات، جرب حذف مجلد `node_modules` وإعادة تثبيت التبعيات
3. **مشاكل المنفذ**: Glitch يستخدم المنفذ الذي يحدده في متغير البيئة `PORT`، تأكد من أن الكود يستخدم `process.env.PORT || 3000`

## بيانات تسجيل الدخول الافتراضية

### المشرف
- اسم المستخدم: admin
- كلمة المرور: admin123

### الطالب
- رقم القيد: 123456
- كلمة المرور: student123

## روابط مفيدة

- [توثيق Glitch](https://glitch.com/help/)
- [مجتمع Glitch](https://support.glitch.com/)
- [مدونة Glitch](https://blog.glitch.com/)
